const int workchain = 0;

() force_chain(slice addr) impure {
  (int wc, _) = parse_std_addr(addr);
  throw_unless(333, wc == workchain);
}

slice null_addr() asm "b{00} PUSHSLICE";

const int min_balance = 10000000; ;; 0.01 TON